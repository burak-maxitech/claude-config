.DS_Store
*.swp
*~

# Machine-local Claude Code settings (permissions, etc.) — each machine keeps its own
.claude/settings.local.json

# skill-creator eval scaffolding (fixtures, iteration runs, benchmarks) — throwaway
.skill-creator-workspace/
