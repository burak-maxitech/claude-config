# Session History Archive

> Auto-managed by `/update-docs`. Last session summary is in [CLAUDE.md](../CLAUDE.md).
> **Note:** Sessions older than the 5 most recent are compressed to one-liners with commit hashes. Full prose for compressed sessions lives in git history (`git show <hash>`).

---

### Session 1 - 2026-03-10: REFACTOR mode — created CLAUDE.md (10 required sections), `docs/` folder with session-history/key-decisions/completed-work, added Documentation section to README. (commits: 246c001, 9981d1e)

### Session 2 - 2026-03-12: `/code-cleanup` polish — `--dry-run`, `--aggressive` clarification, `Bash(gh:*)` permission; Python package→import-name lookup table (30+); batched `git log` for one-off script detection; conservative media-query flagging. (bundled in 639cfb3)

### Session 3 - 2026-03-12: Major skill overhaul — `/plan-feature` phase gating + test types + commit-per-phase + rollback strategy; `/code-review` `--verify`/`--security`/`--fix` modes + git blame context + large diff guard; new `references/security-deep-dive.md` with OWASP Top 10. (commit: 639cfb3)

### Session 4 - 2026-03-12: `/resume-work` + `/update-docs` robustness — bidirectional task integrity (pre-hydration stale check + post-drain validation), context freshness detection vs git, commit checkpoint with `--skip-commit`, compact tip, deep-mode health check. (commits: 639cfb3, f616e0d)

### Session 5 - 2026-03-12: Startup scripts — `start-claude.sh` (Mac/Linux) and `start-claude.ps1` (Windows) automating 5-step session startup; Workflow.md and README.md updated with Quick Start; manual steps preserved alongside. (commits: acfc2b9, f24fd37, f7d3342)

### Session 6 - 2026-03-12: PowerShell script filename fix (`Start-ClaudeSession.ps1` → `start-claude.ps1`) in Workflow.md and README.md; added `Unblock-File` Windows first-run note. (commit: e0c6a49)

### Session 7 - 2026-03-12: Startup scripts no longer auto-run `/resume-work` — show a tip message instead so user controls when to load context; remaining PS1 filename refs cleaned up. (commit: f58e5eb)

### Session 8 - 2026-03-13: Fixed wrong symlink-fix paths in `start-claude.sh` (referenced non-existent `commands/`); added `chmod +x` Mac/Linux first-run note; flipped `update-docs` `disable-model-invocation` to `false` for programmatic invocation. (commits: dc01f2d, 4a6cea4)

### Session 9 - 2026-03-13: Clarified shell-alias instructions in Workflow.md — explicit "paste into config file" wording with `source ~/.zshrc` and `notepad $PROFILE` reload hints. (commit: 0233b79)

### Session 10 - 2026-03-13: Workflow.md cleanup — removed 60-line duplicate setup section (replaced with link to README); alias name fix `claude-start` → `cc`; reordered Quick Start first with manual steps in `<details>`; moved alias setup one-liners to README; removed `claude-config` filter from project picker in both startup scripts. (commits: 7c99471, 777ab86)

### Session 11 - 2026-03-17: Enabled model invocation on plan-feature skill (`disable-model-invocation: false`). (commits: 734b13b, 1cffa6b)

### Session 12 - 2026-04-11: CC 2.1 feature audit — killed `--gated` skill flag after verifying `defer` PreToolUse only works on single-tool-call turns for external SDK callers; shipped README interop section + skill doc notes for CI gating / MCP `maxResultSizeChars` / plugin `bin/` detection. (commits: 56a5513, dd6a7ce, 644fb0c, 83f9bb1)

### Session 13 - 2026-04-11: External best-practice repo review — direct-fetch verification killed 2 of plan's Tier 2 candidates (`paths:`/`effort:` were doc-extrapolation, not observed in actual external skills); shipped 3 doc bullets (MCP setup in README, mid-session `/compact` in workflow, `/loop` reference); flagged `.claude/rules/` symlink-friendly rule files as future option. (commits: adf634e, 5f61209, 445c357)

### Session 14 - 2026-04-14: Best-practices doc audit + 4 skill alignments — `/plan-feature` Step 0 triviality check + `AskUserQuestion`-driven interview, `/code-review` fresh-session tip, `/code-cleanup`+`/code-review` `--fix` `/rewind` recovery footer; post-commit `/simplify` collapsed Step 5 duplication. (commits: a94fbda, 4d444b0, 7e3f6fb)

### Session 15 - 2026-04-16: Opus 4.7 alignment — `effort: high` on `/code-review` and `/plan-feature`; positioned `/ultrareview` as complementary; Sonnet-pin re-confirmed at price parity; dropped S12 `defer` dogfood; designed session-history rollup Part 6 + first-run consent gate using `AskUserQuestion`. (commit: bc8e40b)

### Session 16 - 2026-04-17: /update-docs perf — `effort: low` pin + Part 3.0 parallel batch-Read of `docs/*.md` + Part 6.3 single pre-fetched `git log` across compressible date range; `/resume-work` also pinned `effort: low`; CLAUDE.md decision rows refreshed; first run dogfooded the new parallel-read path. (commit: af0c451)

### Session 17 - 2026-04-21: active CLAUDE.md cap enforcement (Part 1.10) + first-run-gated Key Decisions rollup (new Part 6, FIFO from topmost) + Parts reordered so commit checkpoint is truly last; Pruning-Is-Preservation doctrine added to `doc-structure-rules.md`; same-session dogfood rolled up oldest 18 decision rows to `docs/key-decisions.md` and compressed S12 in `docs/session-history.md`. (commits: a8c99ba, dda6690)

### Session 18 - 2026-05-04: /update-docs perf overhaul — `--fast` mode (skips Parts 0.5/2/3/4/5/6/1.10, drift warnings as safety valve); Step 0 upfront parallel batch (single turn reads CLAUDE.md + README + all docs/*.md + MEMORY.md + git log/diff/status + TaskList, replaces 5-6 sequential read turns); plan-then-batch CLAUDE.md edits via single Write or non-overlapping parallel Edits; same-session `--fast` dogfood; deferred levers captured for measurement-driven follow-up (lazy-load heavy mode-update parts, evidence-driven Part 1 sub-sections). (bundled in 3142e42)

### Session 19 - 2026-05-04: /update-docs Full-Path dogfood backfilling 14546ff (interop polish) + ff89cbb (/code-review --verify parallelization); Step 0 batch + plan-then-batch validated end-to-end on Full Path (~3 substantive turns); Part 5 rolled up S13, Part 6 moved 3 oldest Decisions rows via sentinel-gated silent runs. (commits: 3142e42)

### Session 20 - 2026-05-06: /code-cleanup reference perf rewrite — git ls-files over find, batched Grep with regex alternation across unused-files / dead-functions / unused-CSS / unused-vars scans; npm/yarn/pnpm/Cargo monorepo workspace detection in scan-deps-config.md with new "Misplaced Dependencies" output section; mid-edit correctness fix replacing literal `rg ...` shell-command examples with Grep tool syntax (agents have Bash(grep:*) not Bash(rg:*)); Part 5 rolled up S14+S15; Part 6 moved oldest "Startup scripts" decision row. (commits: 9841a85)

### Session 21 - 2026-05-07: /architecture-review skill built (3-dim initial + 4-dim extension same session) — three guardrails (catalog-driven refactors over GoF pattern-mongering, intended-architecture detection from CLAUDE.md/ADRs, CCN-delta sanity gate); 4th `arch-simplification` dimension added mid-session after honest audit found "least-code-possible" was under-served, with mandatory `lines_deletable` per finding and "Code we can delete: N lines across M files" as primary report metric. Subagent count 3→7. (commits: 52635ae, 261878c, 3984607)

### Session 22 - 2026-05-08: /code-health-advice skill built (read-only routing advisor, 5 state buckets A/B/C/D/E with tie-breakers, never invokes/edits) + /code-cleanup --vulns mid-session extension (multi-stack CVE scan: npm/yarn/pnpm/pip-audit/safety/cargo audit/govulncheck/composer/bundle, opt-in only, never auto-fixed, install-hint pattern when tool missing); plan-feature interview-rules.md cap fixed from "3-5" to "3-4" (AskUserQuestion array cap). Skill count 6→7. (commit: d581983)

### Session 23 - 2026-05-13: /test-review skill plan finalized via 12-question /plan-feature interview — 3-way subagent decomposition (test-coverage / test-quality / test-economics), twin headline metric (coverage gaps + deletable tests), T01-T05 smell catalog with project-defined-assertion-helper false-positive guard, --coverage opt-in mirroring /code-cleanup --vulns (jest/vitest/pytest-cov/cargo-tarpaulin/go cover), --fix T01-only carve-out (assertion-free deletion provably safe), non-overlap with /code-cleanup §7 (bidirectional footer pointers). /code-health-advice Buckets C/D/E updated to reference /test-review BEFORE the build (4 Edits across 2 files). 2 oldest CLAUDE.md decision rows rolled up to docs/key-decisions.md per Part 6 FIFO (CI gating + Plugin bin/ helpers). (commit: ed79342)

### Session 24 - 2026-05-14: /test-review skill built end-to-end per S23 plan via /plan-feature (8 phase commits, no feature branch) — 3 Sonnet subagents (test-coverage / test-quality / test-economics); T01-T05 smell catalog with project-defined-assertion-helper allowlist as load-bearing T01 false-positive guard; twin-headline math (deletable_total only includes T01 + confirmed-duplicate T05 + snapshot_heavy economic_signal; T02/T03/T04 excluded as rewritable); --coverage opt-in per-stack table (9 rows: Jest/Vitest/pytest-cov/coverage.py/cargo-tarpaulin/cargo-llvm-cov/go cover/phpunit/JaCoCo); --fix T01-only carve-out with pre-flight abort when helper_allowlist_size missing; cascade routing preserves /code-cleanup boundary (empty file → /code-cleanup, not self-delete); Phase 7b needed for Windows case-quirk on Workflow.md staging. 11 new files (~1,695 LOC) + 2 docs modified. (commits: 51a91d6, a52c3c7, 6eeb332, 9377e06, f22874c, e2a3a3b, 1406c61, d379222)


### Session 25 - 2026-05-14: /seo-review SEO + GEO audit skill built (web-only) via /plan-feature 12-Q interview — 3 Sonnet subagents (seo-technical / seo-content / geo-generative); 100-pt rubric with ±5 weight-tuning + sum=0 gate; --fix strict-allowlist (TODO placeholders, never fabricates copy); sitemap URL probe added mid-build per user catch; 3 new patterns (fetch-best-practices-every-run, orchestrator-only HTTP, in-repo seo-history.md). /code-health-advice Bucket C/E web-conditional routing. /simplify pass found 14 / fixed 10. (commits: dbcb820, 301e9da, bd2c6f7, f9827e9, 59a466a, 46286e4, b6ca5fd)

### Session 26 - 2026-05-14: Fresh-context /code-review on S25 /seo-review build (1000+ line diff) — 3 Important findings (I1 Edit-vs-Write missing for first-run history file, I2 sub-dim caps unenforced, I3 orphan table row in docs/key-decisions.md from S25 Part 6 rollup). Fixed I1 + I2: added Write to allowed-tools; rubric.md per-sub-dim clamp formula canonical, SKILL.md Step 6.1 enforces with divergence-flag fallback. Design decision: clamp lives in orchestrator not subagents (single source of truth, no drift across 7 places). I3 deferred to S27 — investigates Part 6 append-at-EOF bug. 8 deferred /code-review suggestions captured. Part 5 rolled up S18 to one-liner. (commit: d118f4b)


### Session 28 - 2026-05-14: /seo-review v3 BigQuery plan kicked off via /plan-feature after user discovered GSC Bulk Data Export to BigQuery — 12 design Qs across 3 batches, Plan-agent validation surfaced 8 blindspots/4 simplifications/4 ordering improvements (all adopted); pre-interview WebFetch revealed BigQuery export covers Performance ONLY (no Page Indexing), forced v3 hybrid (BQ + CSV); plan paused at task hydration, no code yet (commits land in S29).

### Session 29 - 2026-05-15: /seo-review v3 BigQuery Phases 0-3 shipped, then mid-session pivot to v3.x API after realizing GSC API serves both Performance + Indexing through one auth (12 Qs/3 batches + plan-agent val); user-requested SIMPLIFY commit cut all BigQuery + CSV paths (+437/-1774 net) → API-only architecture. (commits: dedfd75, 7486a62, 3a2aad6, bffc510, bb0fdcf, 385db25, 7ab15b9, 58a1d62, 1feadf0, 95456a0, 6d262dc, 8fba1fa, 51b4685)

### Session 30 - 2026-05-15: /seo-review v3.x dogfooded end-to-end on burakarik.com (first Phase 4 real run) — 3 same-session skill fixes (x-goog-user-project header missing from API calls → 403 SERVICE_DISABLED; jq dependency drop for ADC quota_project_id; cross-cutting Ingestion conventions section with disk-write boundary + JSON parser fallback + budget-utilization rules) + rowLimit cap-hit surfacing; real-world score 40/100 + AI-citation evidence (prompt-injection queries in GSC results confirming ChatGPT/Claude/Perplexity pulling from the smartphone-vs-mirrorless article). (commits: 835800d, ada0353, bc1170a, bc35331, af02e09, aa95b47, 78f7e0f, 03ab5d9, 4aaf500)

### Session 31 - 2026-05-15: /seo-review GSC API response cache shipped — 24h TTL + --no-cache flag, per-Q + per-URL prefixes (sa-q1/sa-q2/sa-q3 + ui-), 5 wrapper invariants (atomic write, never-cache-non-200, find -mmin TTL portability, pre-created dir, status marker), sentinel-gitignore-block extension; /simplify pass surfaced 6 fixes all shipped same-session (Windows correctness top priority). (commit: 87e2edf)

### Session 31 cont. - 2026-05-15: /update-docs Part 7 size-pressure rollup shipped — per-section char-based active enforcement after Parts 5/6 count-based rollups; dogfooded same-session, CLAUDE.md 38k → 14k chars. (commits: 5fba06a, f56d0e3)

### Session 31 cont.² - 2026-05-15: /seo-review dogfooded on burakarik6 4th time → 6 spec gaps caught + fixed same-session — UTF-8 enforcement on Python (Windows charmap crashes on Turkish + prompt-injection queries), same-commit history dedup, gsc-parse-helper.py shipped (250 lines / 6 subcommands canonical parser), sub-dim 13 brand_query_anomaly (entity-recognition deficit signal), CTR opportunity dual trigger (high-volume override for pos<5 catastrophes), probe-skipped redistribution rendering. (commits: 7109213, 5982bb4)

### Session 32 - 2026-05-23
**What happened:**
- **Naming collision triage.** User flagged that today's Anthropic update renamed built-in `/simplify` → `/code-review`, colliding with this repo's custom code-review skill. The available-skills list in-session showed only the built-in description winning the lookup ("Review the current diff for correctness bugs at the given effort level..."), confirming the custom was being shadowed.
- **Recommendation: rename custom skill to `/review-deep`.** The two skills are genuinely different (built-in: lightweight diff scan + effort levels + `--comment` PR-comment posting; custom: senior-engineer review with codebase-convention scanning + `--security` OWASP / `--verify` test-and-lint / `--fix` auto-fix / `--last-commit`). Keeping both is valuable. The custom skill's own SKILL.md already positioned it as a middle tier between built-in lightweight and `/ultrareview` — so `/review-deep` slots into a clean **3-tier ladder**: `/code-review` (built-in, fast, daily driver) → `/review-deep` (custom, thorough) → `/ultrareview` (built-in, cloud, 5+ verifying agents, high-risk pre-merge). The "deep" suffix mirrors the existing `/resume-work deep` flag convention.
- **Refactor executed.** Directory renamed via `git mv .claude/skills/code-review .claude/skills/review-deep` (4 file paths preserved with full git history — SKILL.md + 3 reference files). Frontmatter `name:` field updated. SKILL.md self-references updated (header `# /code-review` → `# /review-deep`; fresh-session tip; description rewritten to lead with tier positioning).
- **Scope expanded mid-refactor.** A grep for `/simplify` surfaced 46 mentions across 11 files. Doing only the custom rename would have left dead references (`/simplify` does not exist by that name anymore — Anthropic's rename is also live today). Reasonable call under auto-mode: do both renames in operational files. Historical files (`docs/session-history.md`, `docs/completed-work.md`, `docs/key-decisions.md`, plus historical sections inside CLAUDE.md — Key Decisions table rows describing past decisions, Last Session blocks describing past sessions) deliberately left untouched as records of past state.
- **Rename rules applied:**
  - References using `--verify`/`--security`/`--fix`/`--last-commit` flags → MUST become `/review-deep` (built-in does not have those flags).
  - Bare `/code-review` references where context describes the thorough senior-engineer tier → became `/review-deep`.
  - Bare `/code-review` references where context describes a lightweight diff scan → kept as `/code-review` (built-in fits — natural default post-rename).
  - `/simplify` references → became `/code-review` (Anthropic's new name).
- **16 operational files modified + 1 directory rename (4 file paths under it).**
- **README "Three review tiers" blockquote** was rewritten to lead with the 2026-05-23 collision context — future readers landing on README will see *why* the rename happened, not just the resulting structure. The blockquote enumerates all three tiers with their distinguishing features, then a comparison table lists all the audit/review skills by scope.
- **`/update-docs` invoked at session end** to capture this work. CLAUDE.md at 16.7k chars pre-update, well under all thresholds — no Part 5/6/7 rollups fire. Single new Key Decision row added. Last Session block replaced.

**Files created/modified:**
- **Renamed (4 file paths via `git mv`):**
  - `.claude/skills/code-review/SKILL.md` → `.claude/skills/review-deep/SKILL.md` (+ frontmatter `name:` field, header, fresh-session tip, description rewritten)
  - `.claude/skills/code-review/references/output-format.md` → `.claude/skills/review-deep/references/output-format.md`
  - `.claude/skills/code-review/references/review-checklist.md` → `.claude/skills/review-deep/references/review-checklist.md`
  - `.claude/skills/code-review/references/security-deep-dive.md` → `.claude/skills/review-deep/references/security-deep-dive.md`
- **Modified (16):**
  - `.claude/agents/arch-structure.md` — `/code-review` per-commit ref expanded to "(or `/review-deep` for thorough)"; `/simplify` "Do NOT flag" line removed (skill no longer exists by that name).
  - `.claude/scripts/start-claude.sh` — `disableSkillShellExecution` warning updated `/code-review --verify` → `/review-deep --verify`.
  - `.claude/scripts/start-claude.ps1` — same as above.
  - `.claude/skills/architecture-review/SKILL.md` — frontmatter description "Different from..." list updated (dropped `/simplify`, added `/review-deep` with flag list); intro distinct-from list restructured to 3-tier ladder; quick-ref table split into quick/thorough rows.
  - `.claude/skills/architecture-review/references/fix-mode.md` — `/code-review --verify` → `/review-deep --verify`.
  - `.claude/skills/code-cleanup/SKILL.md` — frontmatter description "different from /simplify" → "different from /code-review".
  - `.claude/skills/code-health-advice/SKILL.md` — routing list updated (dropped `/simplify`, added `/review-deep`); "Not a reviewer" list same update.
  - `.claude/skills/code-health-advice/references/state-buckets.md` — Bucket A workflow chain: `/simplify → /code-review → /code-review --verify` → `/code-review → /review-deep → /review-deep --verify`; Bucket B `/code-review --security` → `/review-deep --security`; Bucket E gets new clarifier note about bare `/code-review` = built-in.
  - `.claude/skills/review-deep/SKILL.md` — full frontmatter + header + self-reference rewrite (covered under the rename above).
  - `.claude/skills/seo-review/SKILL.md` — distinct-from list `/code-review` row expanded to "`/code-review` / `/review-deep`"; quick-ref table same expansion.
  - `.claude/skills/test-review/SKILL.md` — frontmatter description "Different from /code-review §7" → "/review-deep §7"; intro distinct-from list expanded with built-in mention; suggested-next-actions skill-chain mentions both; quick-ref table split.
  - `.claude/skills/test-review/references/fix-mode-test.md` — `/code-review --verify` → `/review-deep --verify`.
  - `.claude/skills/test-review/references/report-template.md` — sample suggested-action `/code-review --security` → `/review-deep --security`; footer "Defer to /code-review" expanded to "Defer to /code-review or /review-deep".
  - `CLAUDE.md` — file-tree directory name; this /update-docs sweep adds Key Decisions row + Last Session block.
  - `README.md` — file-tree directory name + reordered into alphabetical position; commands table row replaced (`/code-review` → `/review-deep` with rewritten description); "Three review tiers" positioning blockquote rewritten to lead with 2026-05-23 collision context + 3-tier enumeration; "Picking among the review skills" comparison table restructured (dropped `/simplify` row, added `/review-deep` row, updated `/code-review` row to describe built-in); interop section references `code-review --verify`/`code-review --fix` updated to `review-deep --verify`/`review-deep --fix`; MCP `maxResultSizeChars` reference updated to `review-deep` since it was always about the custom skill.
  - `Workflow.md` — Quick Reference table row split into quick/thorough; Code Quality Checks section rewritten with 3-tier intro + new command examples; `/review-deep` command section replaces former `/code-review` section with full flag list including `--security`/`--verify`/`--fix`; `/test-review` "useful chain" reference + advisor table + loop example + troubleshooting row + file-tree all updated; in-scenario `/code-review` invocations in workflow scripts kept (built-in default is fine for routine checks) with comment-line clarifiers added.
- **Appended this session (via /update-docs):**
  - `docs/session-history.md` — this S32 entry.
  - `docs/completed-work.md` — S32 entry + annotation on the original `/code-review` line.
  - `CLAUDE.md` — Key Decisions row + Last Session block replaced.

**Watch-for next session:**
- (a) `/review-deep` actually resolves to the custom skill in a fresh `claude` session — verify by `/help` or by invoking `/review-deep --staged` on a real diff.
- (b) Built-in `/code-review` invocation in a fresh session pulls the Anthropic version (now in available-skills list).
- (c) Skill cross-references in scenarios where ambiguity could surface — e.g., when `/code-health-advice` routes Bucket A, does the rendered output cleanly show `/code-review → /review-deep → /review-deep --verify` or does any stale `/simplify` leak through.
- (d) Any historical reference in CLAUDE.md historical sections (Key Decisions older rows, `docs/key-decisions.md`) that a future `/resume-work` may surface — confirm those do not confuse a fresh reader (they should not; the rename context is now in the freshest Key Decisions row).

**Next session should:**
- Commit this session's refactor + /update-docs sweep as a single commit.
- Resume the dogfood backlog from CLAUDE.md Next Steps: `/test-review` (#1) is the highest-leverage next dogfood — built S24, never run end-to-end.
- If a `/seo-review` re-run on `burakarik6` is desired, ensure a substantive code change ships first (S31 cont.² same-commit dedup will silently skip otherwise).

### Session 33 - 2026-05-23
**What happened:**
- **User asked for top improvement opportunities** in the repo's skills based on current 2026 Claude Code best practices. Asked for web research from reputable sources + a top-5 list ordered by significance.
- **Web research executed.** WebSearch + WebFetch across: official `platform.claude.com` skill-authoring best-practices page, official `code.claude.com` skills doc, hooks guide, subagents doc, plus 2026 community writeups (Tembo, ksred, claudefa, DEV.to, smartscope, codewithseb). Surfaced several new-in-2026 capabilities the repo wasn't using: `when_to_use:` field for trigger-phrase separation, dynamic `` !`<cmd>` `` shell injection at skill-render time, `${CLAUDE_SKILL_DIR}` substitution, `context: fork` + `agent:` for forked-subagent skills, `paths:` glob auto-activation, skill listing budget (`skillListingBudgetFraction`, 1% context default, 1,536-char description cap), `ultrathink` keyword for deep-reasoning turns, SessionStart hook stdout injection.
- **Top 5 identified and presented** with reasoning + concrete file references + significance ranking. User said "lets implement these 5 improvements." Created tasks #6-10 to track each (existing dogfood tasks #1-5 untouched).
- **All 5 implemented in this session** (no follow-ups deferred). 11 modified files + 2 new scripts.
- **Fix #4 — `${CLAUDE_SKILL_DIR}` for `/seo-review`.** Replaced custom `$SKILL_REF_DIR` var (which required orchestrator-side path resolution per run, documented inline) with the official `${CLAUDE_SKILL_DIR}` substitution. Cross-platform, CWD-independent, no orchestrator-side resolution needed. Same bug-class as S30 `jq`-missing and S31 cont.² UTF-8-on-Windows: an environmental assumption the spec underspecified. 1 file touched.
- **Fix #5 — `effort:` audit + `ultrathink` in plan-mode references.** Audit showed `/code-cleanup` was missing `effort:` entirely (added `effort: high`); deliberate `effort: low` retained on `/code-health-advice` (routing advisor, fast), `/resume-work` (IO-bound), `/update-docs` (mostly mechanical). For the 3 `--plan` mode reference files (`architecture-review/references/plan-mode.md`, `test-review/references/plan-mode-test.md`, `seo-review/references/plan-mode-seo.md`), added the literal `ultrathink` keyword at the top with a one-sentence explanation for why deep reasoning helps that specific synthesis. 4 files touched.
- **Fix #2 — `description:` + `when_to_use:` split on 4 verbose skills.** `/seo-review` was the worst offender at ~1130 chars in description alone (the combined `description` + `when_to_use` cap is 1,536 chars; descriptions get truncated lowest-priority-first on overflow). Refactored: lead `description:` with verb + trigger keywords (≤200 chars), push tier-positioning + trigger phrases + "distinct from..." comparisons to `when_to_use:`. 4 files touched (`/seo-review`, `/architecture-review`, `/test-review`, `/review-deep`).
- **Fix #1 — Dynamic `` !`<cmd>` `` injection.** Two skills affected: `/code-health-advice` Step 1 was entirely deterministic git + CLAUDE.md + web-signal commands — perfect candidate. Rewrote Step 1 with 4 inline `!`-blocks (git state, recent commits, gh PR, CLAUDE.md sections, web-signal probe). The skill body now arrives at Claude pre-rendered with current data; no parallel-Bash turn needed. `/resume-work` Step 2 partially injected (4 git commands → inline; Step 1 doc reads stay explicit Read tool calls because they're conditional on file existence — bash globbing would emit empty output and Claude wouldn't know to skip vs continue). Saves 1 turn per invocation each. Aligns with CLAUDE.md `Next Steps #4` deferred-refactor item ("biggest runtime win"). 2 files touched.
- **Fix #3 — SessionStart hook bundle.** Created two new portable scripts in `.claude/scripts/`: `session-start-context.sh` (Git Bash / Mac / Linux) and `session-start-context.ps1` (Windows PowerShell). Both emit cheap (<1s) read-only project orientation: branch, uncommitted-file count, last-commit age, 5 most recent commits, CLAUDE.md Current Status section + Last Updated line + stale-doc warning (commits newer than CLAUDE.md by >2 days), open-PR title/number. Silent on non-git dirs. Added README "Optional: SessionStart hook for auto-orientation" section above the Interop section with full install recipe (per-project `.claude/settings.json` + global `~/.claude/settings.json`) and the "Why a hook *and* `/resume-work`" rationale (deliberate dual-path: hook for routine starts, slash command for deep orientation + task hydration). User memory rule preserved: "Don't remove manual steps when adding automated alternatives" — `/resume-work` stays fully visible. 3 files: `.sh` (new), `.ps1` (new), `README.md` (recipe section added).

**Decisions / non-obvious calls:**
- **Did NOT extend dynamic injection to `/code-cleanup`, `/architecture-review`, `/test-review`, `/seo-review` Step 0 detection blocks.** Those Step 0 blocks mix deterministic file-existence checks with conditional Reads — partial injection would be confusing. Deferred until `/code-health-advice` dogfood (Next Steps #3) validates the pattern in production. This is the "measured first pass" approach: ship the two clearest wins, learn from dogfood, then expand.
- **Did NOT migrate to plugin packaging** (was in the top-10 candidate list but cut from top-5). README explicitly chose standalone for personal workflows, and official docs back that choice. Non-issue.
- **Did NOT add `paths:` glob auto-activation** to any skill. All 9 skills in this repo are explicit workflow commands (user types `/skill-name`), not file-context companions. Wrong fit.
- **Did NOT use `context: fork`** on the orchestrator skills. The multi-subagent skills (`/code-cleanup`, `/architecture-review`, `/test-review`, `/seo-review`) need the orchestrator in the main context to consolidate findings + render reports — `context: fork` is the wrong primitive at the orchestrator level. Could apply to a hypothetical future single-shot extraction skill.
- **Did NOT auto-install the SessionStart hook globally.** Per the user-memory rule on not removing manual options, the hook ships in the repo with documented install recipes but is not auto-wired into the user's `~/.claude/settings.json`. User decides whether to enable it.
- **Cross-platform shell concern flagged at hand-off.** The dynamic-injection `!`-blocks in `/code-health-advice` and `/resume-work` use bash one-liners (`awk`, `tr`, `wc -l`, `grep -oE`). They're Git-Bash-compatible on Windows (the repo's documented environment) but would fail in cmd.exe or PowerShell native. Matches the existing pattern (the seo-review skill already uses bash one-liners and is documented as Git-Bash-on-Windows). The SessionStart hook ships both `.sh` and `.ps1` variants because the hook gets invoked by the OS shell, not Claude Code's bash sandbox. Skill-body `` !`<cmd>` `` doesn't have parallel-flavor support unless `shell: powershell` is set in frontmatter, which would require maintaining duplicate command flavors.

**Files created/modified:**
- **New (2):**
  - `.claude/scripts/session-start-context.sh` — Git Bash / Mac / Linux SessionStart hook (~55 lines)
  - `.claude/scripts/session-start-context.ps1` — Windows PowerShell counterpart (~65 lines)
- **Modified (11):**
  - `.claude/skills/seo-review/SKILL.md` — Fix #4 (`${CLAUDE_SKILL_DIR}`) + Fix #2 (description/when_to_use split)
  - `.claude/skills/code-cleanup/SKILL.md` — Fix #5 (`effort: high` added; was missing entirely)
  - `.claude/skills/architecture-review/SKILL.md` — Fix #2 (description/when_to_use split)
  - `.claude/skills/architecture-review/references/plan-mode.md` — Fix #5 (`ultrathink` preamble)
  - `.claude/skills/test-review/SKILL.md` — Fix #2 (description/when_to_use split)
  - `.claude/skills/test-review/references/plan-mode-test.md` — Fix #5 (`ultrathink` preamble)
  - `.claude/skills/seo-review/references/plan-mode-seo.md` — Fix #5 (`ultrathink` preamble)
  - `.claude/skills/review-deep/SKILL.md` — Fix #2 (description/when_to_use split)
  - `.claude/skills/code-health-advice/SKILL.md` — Fix #1 (Step 1 fully rewritten as pre-injected `!`-blocks)
  - `.claude/skills/resume-work/SKILL.md` — Fix #1 (Step 2 git commands pre-injected; Step 1 doc reads stay explicit)
  - `README.md` — Fix #3 (new "Optional: SessionStart hook" section above Interop, ~30 lines)
- **Appended this session (via /update-docs):**
  - `docs/session-history.md` — this S33 entry
  - `docs/completed-work.md` — S33 entry (5-improvement batch summary)
  - `CLAUDE.md` — Key Decisions row + Last Session block replaced

**Watch-for next session:**
- (a) `/code-health-advice` first dogfood — does the pre-injected Step 1 actually render cleanly? Specifically does the CLAUDE.md `awk` extraction return the right section bounds when `## In Progress` / `## Next Steps` aren't followed by a section starting with a different letter (the `awk '/^## In Progress/,/^## [^I]/'` pattern is letter-specific). Edge case: a `## Iteration` section after `## In Progress` would prematurely cut. Acceptable for this repo (no such collision) but worth flagging.
- (b) `/resume-work` Step 2 pre-injection — does the orchestrator still correctly route into deep-mode health-check ladder when `[deep]` argument is passed? The Step 2.5 logic depends on parsing `$ARGUMENTS`, which is unchanged, but flow-control around the injection blocks needs validation.
- (c) `/seo-review` `${CLAUDE_SKILL_DIR}` substitution — verify it resolves to the symlinked path correctly on Windows (`~\.claude\skills\seo-review` resolves to the project's `.claude/skills/seo-review` via the symlink set up by `start-claude.ps1`). If `${CLAUDE_SKILL_DIR}` returns the symlink target rather than the symlink itself, the gsc-parse-helper.py path resolves correctly. If it returns the symlink, also fine. Either way should work.
- (d) SessionStart hook on first install — does the script actually fire when wired into `~/.claude/settings.json`? Watch for path quoting on Windows (`~` expansion vs `$env:USERPROFILE`), `gh` availability check (the hook degrades gracefully but worth confirming the degraded output is still clean), and the `disableSkillShellExecution` interaction (hook should still fire — it's a hook, not a skill-internal shell injection — but the README note that documents this should be validated).
- (e) `description:` / `when_to_use:` split on the 4 refactored skills — does Claude actually treat `when_to_use:` content as additional trigger context per the spec, or does it just ignore the field? Per the docs, `when_to_use:` is appended to `description:` in the skill listing for matching purposes. Should validate that the refactored skills still trigger as expected on the same prompts that used to fire them when the long description carried the trigger phrases.

**Next session should:**
- Commit this session's 5-improvement batch as a single feature commit (matter for /update-docs Part 8).
- Resume dogfood backlog (Next Steps #1-5 in CLAUDE.md, unchanged this session): `/test-review` is the highest-leverage next dogfood, but `/code-health-advice` is now a natural follow-up since this session changed its Step 1 substantially — dogfooding it would validate Fix #1 in production.
- If the SessionStart hook is installed globally on the user's machine, every subsequent session would auto-orient — worth observing whether `/resume-work` invocation rate drops over the next few sessions (validates the dual-path design choice).

### Session 34 - 2026-05-26

**What happened:**
- **User reported declining indexed page count on burakarik.com despite ongoing SEO work.** Emotional context: "We're harming our website while trying to improve it." Critical reframe delivered mid-session: nothing in this repo touches burakarik.com — the skill is purely diagnostic. The decline preceded all our work; the S34 changes built better tooling to *see* the problem that was already there.
- **Two GSC "Validation failed" email screenshots provided the diagnostic signal.** Screenshot 1: 838 affected URLs with `coverageState: "Page with redirect"`, climbing from ~350 to 838 since ~4/21/26 inflection. Screenshot 2: 663 affected URLs with `coverageState: "Not found (404)"`, peaked at ~1.2K mid-April then declining. URL samples revealed a clear locale-collision pattern: `/article/<slug>`, `/en/article/<slug>`, `/tr/article/<slug>` (same content reachable under 3 prefixes), similarly for `/photo/*` and `/gallery/*`. Likely root cause: incomplete i18n migration on burakarik.com where some old URLs got proper 301 redirects (→ sub-dim 5 "Page with redirect" 838 case) and others were deleted without redirects (→ sub-dim 4 "Not found (404)" 663 case) — same root cause, two manifestations. Investigation of burakarik.com itself pending in a separate session.
- **`/seo-review` blind-spot identified:** the skill's URL Inspection budget (100/run, 80 by impressions + 20 git-changed) inspected only URLs that already had search traffic. URLs Google had deindexed fell out of `url_impressions_map` (no impressions) → never inspected → user only learned about the deindex when Google emailed. The existing sub-dim catalog was correct for the *symptoms* but the *sampling* missed them.
- **Shipped 5-area extension across 10 files (+662/-67 LOC, uncommitted).** Each fix motivated by the dogfood evidence:
  1. **URL Inspection budget 100 → 200, with new sitemap-orphan slice (100 URLs in sitemap but NOT in `url_impressions_map`, document-order sort)** — document-order chosen over `<lastmod>` desc deliberately because the burakarik6 failure pattern is *stable pages suddenly broken*, not *new pages*. Document-order is also deterministic across runs — required for the snapshot regression diff to compare the same URLs over time.
  2. **Sub-dim 14 `deindex_regression` — orchestrator-emitted (not subagent-emitted) from per-run snapshot diff.** Every run writes `.seo-data/gsc/snapshots/<YYYY-MM-DDTHHMMSS>-<commit_sha7>.json` with `{url → coverageState}` map + metadata (run_timestamp, commit_sha, site_url, inspection_count). Next run diffs against the prior snapshot via new `gsc-parse-helper.py regression` subcommand. Transitions classified by severity (server_error → critical; broken_url / redirect / canonical / quality / crawl_budget → high; access → medium; recovery → info footer). Path-cluster detection groups transitions by 1-3 segment prefixes. Cross-references with Step 1.5's 35-day git digest for `git_correlation` evidence. Finding title + action explicitly frame to GSC's "Validation failed" / "New reason preventing your pages from being indexed" emails. 30-day rotation (longer than cache's 7-day since snapshots are smaller + load-bearing for regression detection). Inflection-point footer line emitted independently of the finding (count delta per top-3 reason categories).
  3. **Sub-dim 5 (`redirect_hygiene`) severity tiering** — was fixed `medium`; now medium/<50, high/≥50 (matches sub-dim 2/3 pattern). 838 URLs deserves `high`. Plus **locale-prefix-cluster detection** in evidence: split each affected URL's path on `/`, take first segment; when ≥2 distinct locale prefixes each account for ≥30% of affected count (or one locale prefix accounts for ≥60% AND bare-prefix ≥20%), surface as `locale_prefix_clusters: {en: N, tr: M, bare: K}`. Recommendation text branches: default vs. i18n collision (with explicit fix path: pick one canonical pattern → drop non-canonical from sitemap → verify canonical/hreflang → resubmit + request validation in GSC).
  4. **Sub-dim 4 (`not_found_404`) enhancements** — mirror sub-dim 5's locale-prefix-cluster detection. New `co_occurrence_with_sub_dim_5` evidence when both findings share ≥3 path prefixes — this surfaces the "single i18n migration root cause, two symptoms" diagnosis (some old URLs got proper redirects → sub-dim 5; others got dropped without → sub-dim 4). Recommendation text adds i18n-collision branch + explicit user-supplied-URL tag. **Git-changed slice now resolves BOTH `old_path` AND `new_path` from rename commits** — the old_path resolution is what links a 404 URL to the rename that orphaned it.
  5. **`.seo-data/gsc/known-bad-urls.txt` as 4th URL Inspection source** — user-pasted URL list, up to 50/run. One URL per line, `#`-prefixed comments + blank lines skipped, whitespace trimmed, duplicates removed. Workflow: GSC → Indexing → Pages report → click reason → Export → copy URL column into the file → run `/seo-review`. Catches URLs Google still remembers but the codebase no longer serves (e.g., 404s from CMS edits, old slugs, content removed without redirect, renames older than git's 35-day window). Bucket-sharing with sitemap-orphan: user-supplied takes precedence in the shared 100-slot bucket. Dedup precedence: `impressions > git > user-supplied > sitemap-orphan`. Documented in expanded `gsc-setup-readme-template.md` with concrete GSC-export workflow.
- **Cross-cutting invariants preserved**: `score_impact: 0` on all GSC findings including sub-dim 14 — headline /100 score stays comparable across runs; `${CLAUDE_SKILL_DIR}` substitution for helper script paths; UTF-8 enforcement on Python (`PYTHONIOENCODING=utf-8 PYTHONUTF8=1` + explicit `encoding='utf-8'`); disk-write boundary updated to include `snapshots/` as 5th allowed entry under `.seo-data/gsc/`; `.gitignore` sentinel already covers it (`.seo-data/gsc/` is fully ignored).
- **Subagent file (`seo-gsc-insights.md`) updated**: catalog count clarified — subagent emits sub-dims 1-13 from API output; sub-dim 14 is orchestrator-only (snapshot diff happens between runs, lives in orchestrator). URL Inspection capacity 100 → 200.
- **Sitemap parse moved to Step 1.6's Turn 1 batch** so both URL Inspection (Turn 2b) and Step 3.2 URL probe consume the same parsed `sitemap_url_list` — single-parse contract; Step 3.2 skips its own parse when `sitemap_url_list` is already populated.
- **Smoke check passed**: `python -m py_compile gsc-parse-helper.py` clean (exit 0). No remaining stale "100/run" or "13 sub-dim" references after sweep (grep verified across `.claude/skills/seo-review/` + `.claude/agents/`).
- **`gsc-parse-helper.py` extended**: 2 new subcommands. `snapshot-write <cache_dir> <run_ts> <commit_sha> <site_url> <output_path>` walks `ui-*.json` files in cache, extracts `{url: coverageState}`, wraps with metadata, atomic write via `os.replace`. `regression <current_snapshot> <previous_snapshot>` diffs two snapshots, emits machine-parseable transitions + path_clusters + count_deltas. Two helper functions added: `classify_transition` (maps current coverageState to transition class) and `compute_path_clusters` (groups URLs by 1-3 segment path prefixes).
- **No work on burakarik.com itself this session** — skill is the diagnostic tool; the actual fix (probably: pick canonical URL pattern, regenerate sitemap to match, add 301s for the 663 deindexed URLs to canonical equivalents, request GSC validation) needs to happen in a separate session against the burakarik.com repo. Indexed-page recovery typically takes Google 4-8 weeks even after a perfect fix.

**Files created/modified:**
- `.claude/skills/seo-review/SKILL.md` — Turn 1 sitemap + known-bad-urls Read additions, Pre-Turn-2 parses for both, new Step 1.6.13 snapshot+regression (5 sub-steps), budget bumps in Turn 2b, sub-dim count clarifications throughout, disk-write boundary update to include `snapshots/` and `known-bad-urls.txt` (+156 lines net)
- `.claude/skills/seo-review/references/gsc-ingestion.md` — sub-dim 14 catalog entry (transition table + finding shape + recommended_action tiers + path-cluster algorithm + inflection-point footer line), sub-dim 5 tiering + locale clusters + i18n-collision recommendation, sub-dim 4 cross-link + locale clusters + `co_occurrence_with_sub_dim_5` evidence + rename old_paths note, hard rules update (budget + retention), header count 13 → 14 (+107 lines)
- `.claude/skills/seo-review/references/gsc-api-queries.md` — 4-slice selection algorithm (impressions / git / user-supplied / sitemap-orphan) with dedup precedence + rationale, rename old_paths in git-changed slice, pre-flight budget log examples for both with-user-file and without, capacity updates throughout (+59/-19 lines)
- `.claude/skills/seo-review/references/gsc-parse-helper.py` — new `snapshot-write` + `regression` subcommands, `classify_transition` + `compute_path_clusters` helpers, updated docstring + `main()` dispatch with 5-arg validation for snapshot-write (+261 lines, 0 deleted)
- `.claude/skills/seo-review/references/rubric.md` — sub-dim 14 row in catalog table (with orchestrator-emitted note + S34 codification context), 13 → 14 sub-dimensions header (+5 lines)
- `.claude/skills/seo-review/references/report-template.md` — new "Index Coverage regression" sub-block in Section 3 with rendered examples for both "transitions detected" and "no transitions" cases + first-run handling, 6 → 7 sub-blocks header (+60 lines)
- `.claude/skills/seo-review/references/gsc-cache.md` — cache-vs-snapshots distinction table (purpose / retention / TTL / file naming / managed by / reproducibility), capacity updates throughout (+19 lines)
- `.claude/skills/seo-review/references/gsc-api-schema.md` — endpoint capacity 100 → 200 with 3-slice mention, sub-dim count 12 → 14 in header (+4 lines)
- `.claude/skills/seo-review/references/gsc-setup-readme-template.md` — new optional `known-bad-urls.txt` documentation block (~50 lines: format, how-to-populate via GSC export, caps, when-to-delete), folder layout update including `snapshots/`, capacity updates (+50 lines)
- `.claude/agents/seo-gsc-insights.md` — sub-dim count clarification (1-13 subagent vs 14 orchestrator), URL Inspection capacity 100 → 200 with 3-slice mention (+8 lines)

**Files NOT modified (and why):**
- `README.md` / `Workflow.md` — no public-surface changes; skill internals only. The `/seo-review` command surface (flags, allowed-tools) didn't change.
- `MEMORY.md` (auto-memory) — stable facts unchanged (skill count still 9, subagent count still 13 — sub-dim 14 is internal to existing `/seo-review`).
- Any code in burakarik6 — separate repo; the actual fix happens in a separate session.

**Next session should:**
- **Run `/seo-review` on burakarik.com to validate S34 extensions.** Recommended: populate `.seo-data/gsc/known-bad-urls.txt` with URLs from both GSC emails before running (50 URLs per run, can split across 2 runs to cover the full 838 + 663). Confirm sub-dim 4 + sub-dim 5 fire with `co_occurrence_with_sub_dim_5` evidence + the i18n migration recommendation text surfaces. Sub-dim 14 won't fire on the first run (no prior snapshot) — footer line should note activation on next run.
- **Investigate the burakarik.com codebase directly** for the actual fix: sitemap contents (locale-prefixed `/en/*` + `/tr/*` only, or also bare `/article/*`?), `<link rel="canonical">` tag values on indexed pages, hreflang configuration, git log around 4/21/26 for the routing change that caused the inflection. Fix path: pick one canonical URL pattern (almost certainly locale-prefixed), regenerate sitemap to contain only canonical URLs, add 301 redirects for the 663 deindexed URLs to their canonical equivalents, request validation in GSC.
- **Caveat for the user:** even with a perfect fix, Google takes 4-8 weeks to update the index. The new sub-dim 14 regression detection lets next-run comparisons measure recovery week-over-week instead of guessing.
- **Commit decision pending:** S34 work is uncommitted (10 files modified). User asked at session end whether to commit + open new session on burakarik.com next. Recommended: commit + `/update-docs` (this session) → new session on burakarik.com to validate.
- **Deferred (Part 5 rollup):** 4 sessions (S28-S31) eligible for compression to one-liners. Skipped this run to keep S34 update minimal during a stressful session. Next `/update-docs` should pick them up — the sentinel note is present in `session-history.md` so it will proceed silently.

### Session 34 cont. - 2026-05-26: API-limitation reframe in docs based on user feedback

**Background:** the S34 5-area extension shipped and got captured by /update-docs. User then read the new `gsc-setup-readme-template.md` content and asked sharp follow-up questions that surfaced docs gaps:

- "why am i curating the list and build a file that named 'known-bad-urls.txt'? Are we not reading these rows with the API Calls anyways? I'm so confused. these errors are the ones with 'Page with redirect' and 'Not found 404' rows of the Indexing→Page menu. Doesn't API return the details of these reports?"
- "even if i want to populate, there are multiple reasons for bad urls. how do I tell you the difference? this is a flat file not an excel file with columns. it may be due to 404, or redirect, etc.."
- "how old records should i consider? only today and yesterday to be on the safe side?"

The first question exposed a real docs gap: the original S34 README content framed `known-bad-urls.txt` as the **primary workflow** for handling Google-emailed coverage issues, when it's actually a **fallback for the gap** that the three algorithmic slices (impressions / git / sitemap-orphan) can't cover. The user reasonably assumed the API exposed the Page Indexing report's URL lists — but it doesn't.

**What happened:**

- **Reframed `gsc-setup-readme-template.md`** — major rewrite of the "Optional: `known-bad-urls.txt`" section. New structure: (1) **leads with "the Search Console API does NOT expose the Page Indexing report"** + endpoint comparison table showing what each API endpoint returns vs. doesn't (`searchanalytics.query` / `urlInspection.index.inspect` / `sites.list` / `sitemaps.list`); (2) explains the 4-slice URL Inspection workaround with per-slice table; (3) **"When you need it / When you DON'T need it"** lists — sitemap-orphan handles most "Page with redirect" cases (URLs in sitemap) and "Submitted URL not found (404)" cases (also in sitemap); user-supplied file is for genuine gaps only (URLs Google remembers but the codebase no longer serves, older renames, CMS edits not in git); (4) **"Recommended workflow: try without the file first"** — run empty once, review which URLs were inspected via the footer budget log, only paste URLs that fell through. The "no `pageIndexing.list` endpoint" framing made it clear this is a Google product decision, not a skill limitation.

- **Reframed `gsc-api-queries.md`** — added the API-limitation root-cause + "use as fallback, not primary workflow" guidance to the user-supplied slice description in the source allocation table. Future readers of the spec see the same framing.

- **Added "the skill discovers the reason automatically" clarifying paragraph** to `gsc-setup-readme-template.md`'s Format section (per user request after they asked about reason-tagging in a flat file). Explains: (a) URLs of mixed reasons (404 + redirect + canonical-conflict + etc.) can sit in the same flat file; (b) `urlInspection.index.inspect` returns the current `coverageState` per URL on every API call; (c) the skill maps that to the right sub-dimension via the 9-reason lookup table in `gsc-api-queries.md`; (d) findings get clustered by reason automatically into sub-dims 2-9. Includes a mixed-source example with `#` comments showing how a user might track different GSC export batches. **State-drift caveat:** a URL pasted under one reason may come back under another (or as `"Submitted and indexed"` = resolved) because the API returns Google's *current* view, not the historical reason from when the user exported.

- **Recency guidance settled (conversation-only, not codified):** user asked "should i only consider URLs from today/yesterday to be safe?" Answer: recency is NOT a safety question — `urlInspection.index.inspect` returns Google's CURRENT view per URL regardless of when the URL was first detected. Practical prioritization: sort GSC export by "Last crawled" desc, take top 50 (highest probability the URL is still affected today). Stale entries are harmless (no finding emitted, user trims file). Stale-state cases (URL fixed but Google hasn't recrawled) are actually USEFUL — they surface "request re-indexing" candidates. This guidance is in the conversation but didn't get codified into the README — could go in if it becomes a recurring question.

- **Curated-file review for the user:** user pasted their 100-URL `known-bad-urls.txt` (top 50 from 404 + top 50 from "Page with redirect") for sanity check before the burakarik.com run. Flagged 4 issues:
  1. **Critical: every URL was followed by a date line** like "May 22, 2026" — would silently parse-skip 100 times with the spec-defined "Skipped N lines from known-bad-urls.txt that didn't look like URLs" footer note. User must remove the date lines or convert them to per-URL `#` comments on separate lines.
  2. **100 URLs exceeds the 50/run cap** — only first 50 (the 404 list) would be inspected this run; the 50 redirect URLs would be skipped.
  3. **Recommendation: drop the redirect URLs entirely from the file** since sitemap-orphan slice catches them automatically (they're in current sitemap by definition of "Page with redirect"). Keep only the 50 404 URLs which are the genuine gap. User accepted.
  4. Minor encoding flags noted: `san+francisco` (URL-encoded space), `chèvre-d'or` (typographic apostrophe U+2019), Turkish characters (ş, ğ, ü, ı, ö, ç — UTF-8 paths handled via existing helper-script `PYTHONIOENCODING=utf-8` enforcement), and `http://www.burakarik.com/index.php` (HTTP + www subdomain — works because property is `sc-domain:burakarik.com` which covers all subdomains/protocols).
- Provided cleaned-up 50-URL version (no dates, header comment, 404-only); user accepted the recommendation.

**Pattern observation from the curated 404 list (informs the actual burakarik.com fix):** confirms the i18n collision hypothesis exactly. Same article slug appears under 3 prefixes: `/article/<slug>` (bare), `/en/article/<slug>`, `/tr/article/<slug>`. AND two slug languages cross-pollinating: `/tr/article/dslr-vs-mirrorless-...` (English slug under TR prefix — likely from before translation), `/en/article/dslr-mi-aynasiz-mi` (Turkish slug under EN prefix — same issue in reverse). This is exactly what sub-dim 4's new `co_occurrence_with_sub_dim_5` + locale-prefix-cluster evidence will surface in the report when run.

**Files modified (S34 cont., on top of S34 originals):**

- `.claude/skills/seo-review/references/gsc-setup-readme-template.md` — major rewrite of the `known-bad-urls.txt` section (~120 lines net): API-limitation framing first, 4-slice workaround table, when-need / when-DON'T lists, recommended-workflow paragraph, flat-list-API-discovers-reason clarification with mixed-source example + state-drift caveat. **This is the 11th modified file in the S34 set** (10 original + this one modified twice now in S34).
- `.claude/skills/seo-review/references/gsc-api-queries.md` — added API-limitation root-cause + "fallback not primary" guidance to user-supplied slice description in source allocation table.

**Files NOT modified (S34 cont.):**

- All other skill files — no logic changes; cont. work was purely docs-side reframing.
- `MEMORY.md`, `README.md`, `Workflow.md` — no public-surface changes.
- No new `key-decisions.md` row — this is doc clarification of an existing decision (the S34 row), not a new architectural decision.

**Next session should (unchanged from original S34 + cont.):**

- Run `/seo-review` on burakarik.com with the cleaned-up 50-URL `known-bad-urls.txt` (404-only, no redirects since sitemap-orphan covers them). User has already curated the file and accepted the cleanup recommendation; ready to switch sessions.
- Validate that sub-dim 4 + sub-dim 5 + `co_occurrence_with_sub_dim_5` evidence fires + the i18n migration recommendation text surfaces. Sub-dim 14 won't fire on first run (no prior snapshot); footer line will note activation for next run.
- After the validation run completes, investigate burakarik.com's codebase directly for the actual fix (sitemap contents, canonical tags, hreflang config, git log around 4/21/26 inflection).
- **Part 5 rollup priority:** 5 sessions now eligible (S28, S29, S30, S31, S31 cont.). Next `/update-docs` should compress these FIRST before other work — gets `session-history.md` under the 5-entry top-of-prose cap.



### Session 35 - 2026-05-26

**What happened:**

Same-day continuation of S34 burakarik.com dogfood. User ran `/seo-review` on burakarik6 with the new `known-bad-urls.txt` populated, hit two issues mid-run (orchestrator wrote a 396-line `_inspect_batch.py` into `.seo-data/gsc/` violating the disk-write boundary; only 50 of 100 pasted URLs were inspected due to per-run cap). Triaged into 4 improvement groups, shipped all 4, then ran `/code-review` extra-high effort which surfaced 15 findings, then shipped all 15 fixes same-session. Net: +1105/-71 LOC across 7 files (all uncommitted).

- **Group A — Boundary cleanup + `inspect-batch` helper subcommand.** Codified a broader disk-write boundary rule in SKILL.md: no orchestrator-written scripts (Python/JS/shell) anywhere under `.seo-data/gsc/` for any purpose, regardless of size or whether deleted after use. Added canonical-paths table for the 3 common operations (JSON parsing → existing helper subcommands; URL Inspection dispatch → new helper; ephemeral scratch → mktemp). Shipped `inspect-batch` helper subcommand (~200 LOC: `ThreadPoolExecutor` parallel HTTP, per-URL cache check with 7d TTL, atomic write via `.tmp.<pid>` + `os.replace`, never-cache-non-200) — closes the spec-shaped hole that S31 cont.² + S34 dogfoods kept filling with ad-hoc Python files. Updated SKILL.md Step 1.6.6 Turn 2b from "N parallel Bash curl calls" to a single Bash invocation of the helper. Deleted the orchestrator's `_inspect_batch.py` (396 lines) + 604KB of stale pre-S31-era JSON dumps (`gsc_q1_queries.json`, `gsc_q2_pages.json`, `gsc_q3_urlmap.json`, `inspect/`) from burakarik6's `.seo-data/gsc/`.

- **Group B — Subagent-skip rule codified (Step 4.5).** Codified the "skip codebase-scanning subagents when no non-doc commits since last snapshot" improvisation the orchestrator made during the S34 dogfood. 4 gating conditions (all must hold): GSC mode enabled + prior snapshot exists + zero code commits in scope since prior snapshot + no `--force-dispatch`/`--plan`/`--fix` flags. New `--force-dispatch` flag for explicit override. Audit-trail requirements: per-dim rows must be marked `(inherited from <sha7> [<date>])`, findings tagged `source_status: "inherited"`, footer line explicit, `docs/seo-history.md` row gets `inherited_from:<prior_sha>` HTML comment. Inheritance source is `docs/seo-history.md` (existing file — no new `breakdown.json` to maintain).

- **Group C — Cache TTL split.** Codified split TTL by call-type after S34 dogfood scored 0/197 cache hits despite 85 prior cache entries (3 days old, past the 24h TTL): `sa-*` (Search Analytics) stays 24h (rolling-date keys shift daily anyway); `ui-*` (URL Inspection) becomes 7d (coverageState is weeks-stable). Bash wrapper updated with `case` statement on filename prefix. Eviction also split: `find -name 'sa-*' -mtime +7` + `find -name 'ui-*' -mtime +14` (TTL + slack per tier).

- **Group D — Finding lifecycle (history + watchpoints).** Two new `.seo-data/gsc/` files: `finding-history.json` (skill-managed per-finding `run_count` tracker, hash by `sub_dim|location`, `ESCALATE` marker at run_count≥3) + `watchpoints.json` (auto-emitted when a finding has `code_changed_since_gsc_window=true`, captures baseline metric, 21-day recheck window, 90-day auto-evict). Three new helper subcommands: `history-update`, `watchpoint-emit`, `watchpoint-check`. New Step 1.6.14 (watchpoint-check at Turn 1, AFTER Q2 cache lands), new Step 6.8 (history-update + watchpoint-emit after Step 6.7 noise filter), new Step 7 watchpoint banner format (4 variants: improved/regressed/unchanged/no_data). End-to-end smoke-tested: 3-run sequence correctly triggers escalation; watchpoint detects CTR improvement (0.3% → 1.2%).

- **`/code-review` extra-high pass surfaced 15 findings; all 15 fixed same-session.** 9 finder agents (5 correctness + 3 cleanup + 1 altitude) ran in parallel, deduped + verified, 1 sweep pass for gaps. Top correctness bugs: (1) Step 1.6.14's `ls sa-q2-*.json | head -1` picked random old Q2 file by alphabetical sha1 sort with multi-day cache accumulation — fixed by deterministic hash recomputation from cache-key inputs; (2) `_add_days`/`_parse_date` used `time.mktime`/`time.localtime` (local-time semantics) on UTC dates from orchestrator → DST off-by-one on watchpoint recheck dates and 90-day evictions — replaced with `datetime.date.fromisoformat() + timedelta()`; (3) `_watchpoint_status` crashed with TypeError when Q2 returned `None` for any metric — filter None from `current` dict upstream; (4) docs-spec drift: 5 files still said "3-slice URL Inspection" after S34 added the 4th user-supplied slice — bulk update to "4-slice"; (5) operator precedence in `classify_transition` (`'soft' not in s` guarded only `'404'` branch, not `'not found'`) → parenthesized correctly; (6) `history_update` had no same-commit guard → S31-style methodology-variance reruns would inflate run_count to bogus ESCALATE within hours → added `if commit_sha == entry.last_seen_commit: skip increment`; (7) `_atomic_write_json` used bare `.tmp` suffix → concurrent /seo-review processes could race on shared tmpfile → PID-suffixed; (8) `inspect_batch` had 20-worker ThreadPoolExecutor with no rate limit → would burst over GSC's ~10/sec limit → lowered to 6 workers + 3-retry exponential backoff (2s/4s/8s + deterministic jitter); (9) `_atomic_write_json` `sys.exit(1)` on OSError aborted before stdout flush → silently dropped feature output → returns `bool`, callers print results BEFORE write; (10) Step 6.8 invocations had no `|| skip` error trap → history-update failure could leave watchpoint-emit running on same JSONL → wrapped each in `if ! ...; then skip; fi`; (11) `f.get('title', '')[:120]` crashed on `"title": null` → `(f.get('title') or '')[:120]`; (12) Turn 1 line 139 referenced non-existent "Pre-Turn-2 watchpoint check" addendum → changed to "see Step 1.6.14"; (13) per-call CACHE_STATUS spec drift → split spec into Turn 2a (per-call) vs Turn 2b (aggregate-only); (14) 24h-TTL back-references in 3 places → updated to "split TTL: 24h on sa-*, 7d on ui-*"; (15) `_atomic_write_json` duplicated `snapshot_write`'s inline atomic-write block → refactored `snapshot_write` to call the new helper.

- **Defensive additions beyond the 15:** `wp_doc['watchpoints']` + `history['findings']` get `isinstance(..., list/dict)` checks (handles corrupted JSON); accept full 2xx range (`200 <= http_code < 300`); Q2 cache unreadable emits `WARN: Q2 cache at '...' unreadable` to stderr.

**Files modified (all uncommitted):**
- `.claude/skills/seo-review/SKILL.md` (+357 / -25 net)
- `.claude/skills/seo-review/references/gsc-parse-helper.py` (+736 / +0 — 5 new subcommands: inspect-batch, history-update, watchpoint-emit, watchpoint-check + snapshot_write refactor)
- `.claude/skills/seo-review/references/gsc-cache.md` (+~50 / -7 — TTL split, scope-clarified, two-tier eviction)
- `.claude/skills/seo-review/references/gsc-api-queries.md` (+~20 / -~6 — 50→100 cap, sitemap-orphan caveat, 4-slice)
- `.claude/skills/seo-review/references/gsc-ingestion.md` (+~3 / -~1 — hard rule + 4-slice)
- `.claude/skills/seo-review/references/gsc-api-schema.md` (+1 / -1 — 4-slice)
- `.claude/skills/seo-review/references/gsc-setup-readme-template.md` (+~12 / -~4 — cap raise + sitemap-orphan misconception fix + 4-slice)

**Next session should:**
- **Commit S35 changes.** Suggested: `feat(seo-review): S35 — Groups A-D from burakarik6 dogfood + 15 code-review fixes`. Currently uncommitted.
- **Validate S35 on a real burakarik.com run.** The S34 next-step "Validate on burakarik.com" now has S35's infrastructure backing it: 100 user-supplied URL cap (was 50), helper-driven Turn 2b dispatch, split TTL (ui-* 7d means S34's cache entries from S34 would now hit), finding-history infrastructure ready to track first-seen for all current findings. Skip-mode (`dispatch_mode == "skip_codebase_subagents"`) likely fires on the next same-property run if no code commits intervene.
- **Dogfood backlog unchanged:** /test-review, /architecture-review, /code-health-advice — still built but not end-to-end invoked.
