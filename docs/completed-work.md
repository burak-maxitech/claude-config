# Completed Work

> Full checklist of completed tasks. Referenced from [CLAUDE.md](../CLAUDE.md).

---

## Skills
- [x] `/resume-work` — Session startup with parallel doc reads, git scans, auto-memory, task hydration
- [x] `/plan-feature` — Feature interview with greenfield/existing modes, Plan Mode, task hydration
- [x] `/update-docs` — Session end with task drain, session archiving, auto-memory sync, CREATE/REFACTOR/UPDATE modes
- [x] `/code-review` — Code review with auto-detect (uncommitted/staged/PR/last-commit), convention scanning, file:line refs
- [x] `/code-cleanup` — Parallel subagent scanning with summary dashboard, Quick Wins, scope filters
- [x] `/code-cleanup` enhancements — Added `--dry-run` mode, clarified `--aggressive` behavior, added `Bash(gh:*)` permission, improved description to distinguish from `/simplify`
- [x] `scan-deps-config` subagent — Added Python package→import name lookup table (30+ common mismatches)
- [x] `scan-files-code` subagent — Optimized one-off script detection to batch `git log` instead of per-file
- [x] `scan-styles-tests` subagent — Made media query dead code detection more conservative (default risk: needs_investigation)
- [x] `/plan-feature` enhancements — Phase gating with verification, test types per phase, commit after each phase, context management reminder, rollback strategy in plan template
- [x] `/code-review` enhancements — Added `--verify` (run tests/lint), `--security` (OWASP Top 10 deep dive), `--fix` (auto-fix simple issues), git blame context for findings, large diff guard (500/1000 line thresholds)
- [x] `security-deep-dive.md` — New reference file with full OWASP Top 10 checklist for `/code-review --security`
- [x] `/resume-work` enhancements — Context freshness detection, quick health check (deep mode), bidirectional task integrity pre-check, uncommitted changes warning in summary, compact tip, expanded allowed-tools for build/test commands
- [x] `/update-docs` enhancements — Drain validation after Part 0, commit checkpoint (Part 5), `--skip-commit` flag, post-verification compact suggestion
- [x] `/code-cleanup` CI gating recipe — Documented PreToolUse `defer` hook integration in Fix Mode; explains the single-tool-call constraint and resume-via-`claude -p --resume` flow (SKILL.md)
- [x] `/code-review` CI gating + MCP `maxResultSizeChars` notes — Step 1 sampling notes the MCP `_meta["anthropic/maxResultSizeChars"]` knob (up to 500K) for larger file returns; Step 6 Auto-Fix mirrors the PreToolUse defer recipe from code-cleanup (SKILL.md)
- [x] `/resume-work` plugin `bin/` detection — Step 2.5 deep-mode health check now prefers `bin/check`, `bin/test`, `bin/ci` helpers shipped by enabled plugins (CC 2.1.91+) over the generic `package.json → Makefile → pyproject.toml → Cargo.toml` ladder (SKILL.md)

## Subagents
- [x] `cleanup-files-code` — Scans for unused files and dead code
- [x] `cleanup-deps-config` — Scans for unused dependencies and config cruft
- [x] `cleanup-styles-tests` — Scans for unused CSS and stale test artifacts

## Infrastructure
- [x] Mac/Linux setup instructions with symlink approach
- [x] Windows setup instructions (PowerShell as Administrator)
- [x] GitHub sync workflow (push/pull across machines)
- [x] Workflow.md — Detailed personal workflow guide
- [x] Startup scripts (`start-claude.sh`, `start-claude.ps1`) — Single-command session startup with interactive project picker
- [x] Fixed PowerShell script filename bug in Workflow.md and README.md, added Unblock-File first-run note
- [x] Changed startup scripts to show tip instead of auto-running `/resume-work`; fixed remaining PS1 filename refs in Workflow.md
- [x] Clarified shell alias instructions in Workflow.md — explicit "paste into config file" wording with reload/open hints
- [x] Deduplicated setup instructions — removed from Workflow.md, consolidated in README.md with alias setup
- [x] Reordered Workflow.md — Quick Start (`cc`) first, manual steps collapsed in `<details>` blocks
- [x] Removed claude-config filter from startup script project pickers

## Documentation
- [x] README.md — Public project overview with setup, commands table, subagents table
- [x] Workflow.md — Personal workflow guide with scenarios, tips, troubleshooting
- [x] CLAUDE.md — AI session context with all 10 required sections
- [x] docs/session-history.md — Session archive
- [x] docs/key-decisions.md — Architectural decisions log
- [x] docs/completed-work.md — This file
- [x] README.md "Interop with Claude Code 2.1 features" section — Documents `disableSkillShellExecution`, plugin `bin/` PATH injection, MCP `maxResultSizeChars` up to 500K, the `PreToolUse defer` recipe for CI gating, and @-mentioned subagents; explicitly notes that standalone skills and installed marketplace plugins coexist per official docs
- [x] README.md MCP server setup bullet — Added to the interop section documenting the three MCP scopes (local/project/user), `.mcp.json` at repo root for project scope, the `claude mcp add --transport {http\|sse\|stdio}` CLI, and tool-search context deferral (Session 13)
- [x] `workflow.md` Mid-Session Context Hygiene subsection — Generalizes the `/compact` habit from the existing resume-work/update-docs suggestions to mid-session use. Softened wording ("earlier rather than later") after verification showed no percentage threshold is in Anthropic docs. Includes the CLAUDE.md-survives-compaction note (Session 13)
- [x] `workflow.md` Recurring tasks `/loop` reference — Documents the built-in `/loop` skill with session-scope + 3-day auto-expire caveats. Explicit non-goal: do not reimplement as a custom skill (Session 13)
- [x] `plan-feature` Step 0 triviality check — Skip the interview for one-sentence changes (typo, log line, rename) per official best-practices doc; new Step 0 in `SKILL.md` plus a renumbered "Start Now" list (Session 14)
- [x] `plan-feature` interview via `AskUserQuestion` — `interview-rules.md` rewritten around the tool with multi-choice + "Other / explain" escape; Step 5 of `SKILL.md` updated; `AskUserQuestion` added to `allowed-tools` frontmatter; falls back to numbered chat Q&A when the tool is unavailable (Session 14)
- [x] `code-review` fresh-session tip — One-line tip in the SKILL.md header recommending `/clear` or a fresh `claude` session for non-trivial reviews per the doc's Writer/Reviewer pattern (Session 14)
- [x] `code-review --fix` and `code-cleanup --fix` `/rewind` recovery footer — Both `--fix` modes now end output with `Esc Esc` / `/rewind` undo guidance per the doc's checkpointing emphasis. For `code-cleanup`, `/rewind` is offered alongside the existing `git branch -D` instruction (coarse vs fine-grained undo) (Session 14)
- [x] Opus 4.7 alignment — `effort: high` frontmatter added to `/code-review` and `/plan-feature` SKILL.md (verified `effort` field syntax against code.claude.com/docs/en/skills); other skills inherit session default (Session 15)
- [x] `/ultrareview` positioning — `/code-review` SKILL.md description and README commands table updated to position custom skill as lightweight in-session daily driver vs built-in `/ultrareview` for high-risk pre-merge. New README blockquote explains the split (Session 15)
- [x] `docs/key-decisions.md:45` Sonnet-pin row refreshed — Re-evaluated at Opus 4.7 launch; documented that pricing parity ($5/$25 per Mtok) keeps Sonnet rationale intact (Session 15)
- [x] `workflow.md` version history — Apr 2026 row added documenting the 4.7 alignment work; "Last updated" bumped from March → April 2026 (Session 15)
- [x] Dropped Session 12 `defer` hook dogfood task — Carried 12→13→14, formally retired in Session 15. Removed the carry-forward bullet from CLAUDE.md so future `/resume-work` sessions stop surfacing it. README recipe at line 177 left in place untested (Session 15)
- [x] Session-history rollup pattern — New Part 6 in `update-docs/references/mode-update.md` auto-compresses sessions older than the 5 most recent into one-liners with commit hashes. Added `--skip-rollup` flag to `update-docs/SKILL.md` argument-hint; new checklist item in `verification-checklists.md`. Step 6.2 first-run confirmation gate uses `AskUserQuestion` (added to allowed-tools) so legacy projects aren't surprised on first run; the rollup-format note acts as the per-project sentinel. One-time pass on Sessions 1-10 shrank `docs/session-history.md` from 27.7KB → 22.0KB (-21%) with full prose preserved in git history (Session 15)
- [x] `/update-docs` performance — `effort: low` frontmatter pin + Part 3.0 preamble directing a parallel batch-Read of all `docs/*.md` before edits + Part 6.3 single pre-fetched `git log` across the compressible date range. Addresses a real ~20-minute runtime the user hit on another project; no functionality changed (Session 16)
- [x] `/resume-work` performance — `effort: low` frontmatter pin. Mechanical work (read docs, run git in parallel, fill summary template, hydrate tasks) doesn't need Opus reasoning; frontmatter scope reverts to session default on skill return (Session 16)
- [x] Active CLAUDE.md cap enforcement — new Part 1.10 in `update-docs/references/mode-update.md`: Current Status ≤10 (collapses `Complete` runs), Next Steps ≤10 (warn), In Progress ≤5 (warn). Replaces the passive 35k-char warning with active per-section caps that run every UPDATE. Gated by `--skip-caps`. Root-cause fix for slow CLAUDE.md growth despite documented caps (Session 17)
- [x] Key Decisions rollup — new Part 6 in `update-docs/references/mode-update.md` mirrors Part 5's session-history rollup. FIFO move of oldest CLAUDE.md Key Decisions rows beyond 20 into `docs/key-decisions.md`, first-run `AskUserQuestion` consent gate with sentinel note, gated by `--skip-decisions-rollup` (Session 17)
- [x] Commit checkpoint moved to Part 7 — commit is now truly last so both rollups (Part 5 session-history, Part 6 key-decisions) land in the same commit. Closes a Session 15 bug where rollup changes could end up uncommitted (Session 17)
- [x] "Pruning is preservation" subsection in `update-docs/references/doc-structure-rules.md` — explicit doctrine giving Part 1.10 and Parts 5/6 canonical backing under the existing "when in doubt, keep it" rule. Moving a row CLAUDE.md → reference file is preservation, not removal (Session 17)
- [x] `update-docs/SKILL.md` argument-hint extended — added `--skip-caps` and `--skip-decisions-rollup` alongside the existing `--skip-memory`/`--skip-tasks`/`--skip-commit`/`--skip-rollup` (Session 17)
- [x] `update-docs/references/verification-checklists.md` — 3 new UPDATE-mode checklist items covering cap enforcement, key-decisions rollup, and commit-last ordering (Session 17)
- [x] `/update-docs --fast` mode — daily-cycle Fast Path with `--fast` flag in argument-hint; runs Parts 0/1.0/1.4-1.5/1.6/1.8/7 + drift probes; skips Parts 0.5/2/3/4/5/6/1.10; surfaces drift via warnings instead of enforcement (Session 18, commit 259f400)
- [x] `/update-docs` Step 0 upfront parallel batch — single parallel turn for all reads + `git log -20` + `git diff --stat HEAD~5` + `git status` + `TaskList`, replacing 5-6 sequential read turns across Parts 0.5/2/3/4/5/6; benefits both Fast and Full paths (Session 18, commit 259f400)
- [x] `/update-docs` Part 1 plan-then-batch preamble — directs the model to gather all CLAUDE.md sub-section changes from cached Step 0 evidence and apply as one Write or non-overlapping parallel Edits; Fast Path uses this by definition, Full Path opts in (Session 18, commit 259f400)
- [x] README defer-recipe `if`-field modernization — uses 2.1.85 `if` field (e.g. `"if": "Bash(rm:*)"`) so PreToolUse defer only fires for matching tool calls (commit `14546ff`, 2026-04-22; backfilled Session 19)
- [x] README auto mode compatibility paragraph — documents 2.1.83+ `defaultMode: "auto"` classifier behavior, plan-tier eligibility, narrow-vs-broad allow-rule retention, and `PermissionDenied` hook fallback added 2.1.89 (commit `14546ff`, 2026-04-22; backfilled Session 19)
- [x] `start-claude.{sh,ps1}` `disableSkillShellExecution` guardrail — warns when this setting is on in `~/.claude/settings.json`, since it would silently break `--fix`/`--verify`/`deep` modes (commit `14546ff`, 2026-04-22; backfilled Session 19)
- [x] `code-cleanup/SKILL.md` and `code-review/SKILL.md` CI-gating prose trim — duplicated text collapsed to one-liner pointer at README recipe (commit `14546ff`, 2026-04-22; backfilled Session 19)
- [x] `/code-review --verify` parallelization — Step 1.5 now launches test/lint with `run_in_background` so they complete during review work (Steps 2-4); Step 5 collects output instead of running synchronously; saves ~10-60s per invocation on non-trivial test suites (commit `ff89cbb`, 2026-04-22; backfilled Session 19)
- [x] Session 19 Full Path validation run — back-to-back `/update-docs` (no flag) immediately after Session 18's `--fast` run; cleared all 4 drift items (Part 5 compressed S13 to one-liner; Part 6 FIFO moved 3 oldest Key Decisions to docs/key-decisions.md; backfilled commits `14546ff` + `ff89cbb`); Full Path performance subjectively far below 12-14 min baseline though best-case (caches warm) (Session 19)
- [x] `/code-cleanup` reference files — perf rewrite: `git ls-files` over `find` (honors `.gitignore` natively, faster on large repos); per-item grep loops in unused-files, dead-functions, unused-classes, unused-CSS-variables, and unused-deps searches collapsed into single batched `Grep` calls with regex alternation (`\b(name1|name2|...)\b` + `output_mode: count`). Applied uniformly across `scan-files-code.md`, `scan-deps-config.md`, `scan-styles-tests.md` (Session 20)
- [x] `/code-cleanup` monorepo coverage — `scan-deps-config.md` §4.0 workspace detection: npm/yarn `package.json` `workspaces`, pnpm `pnpm-workspace.yaml` `packages:`, Cargo root `[workspace] members = [...]`. Per-workspace scan with sibling-doesn't-count rule; output gains `workspace:` field on Unused Dependencies; new "Misplaced Dependencies (monorepos only)" output section. `SKILL.md` Step 0 detection list updated; "Detected:" example expanded to surface monorepo state (Session 20)
- [x] `/code-cleanup` Grep-tool correctness — refactored literal `rg --type js -e "..."` shell-command examples in all three reference files to use the `Grep` tool's parameter shape (`pattern:`, `output_mode:`, `glob:`, `path:`); cleanup-* agents have `Bash(grep:*)` not `Bash(rg:*)`, and runtime guidance pins use of the `Grep` tool (Session 20)
- [x] New `/architecture-review` skill — repo-wide complexity + refactor + perf audit, distinct from diff-scoped reviewers (`/code-review`, `/simplify`, `/ultrareview`) and deletion-focused `/code-cleanup`. Three guardrails: catalog-driven complexity-reducing refactors (not GoF pattern-mongering, R13 Strategy gated by 4 conditions), reads intended architecture from CLAUDE.md/ADRs first to prevent imposing a foreign architecture, CCN delta sanity gate (drops findings where projected ≥ current). 3 parallel Sonnet subagents (`arch-structure`, `arch-refactors`, `arch-performance`). Scale tiers: <100=full, 100-500=bounded, >500=smart sample (LOC × churn × import-fan-in priority + drill-down). `--fix` restricted to single-file non-API-breaking; cross-file auto-routes to `--plan`. Files: `architecture-review/SKILL.md`, 8 references, 3 agents (commit `52635ae`, Session 21)
- [x] `/architecture-review` 4th dimension — `arch-simplification` subagent for over-engineering detection at sub-file granularity (vs `/code-cleanup` whole-file granularity). Catalog S01-S09: single-impl interface inline, pass-through wrapper, always-same parameter, unread/same-value config, defensive code for impossible static states, speculative generic, near-duplicate functions, unused exported symbol. Every finding mandatorily reports `lines_deletable >= 1`. Report opens with **Code we can delete: N lines across M files**. Quick-wins phase puts deletions ahead of refactors. Rank score factors `log(lines_deletable + 1)`. False-positive guards: test seams (mocks legitimize abstractions), boundary types (Adapter/Mapper), recently-added abstractions (<30 days), public-API exports. `--fix` eligible: S04, S06, S09 (single-file); rest auto-route to `--plan`. Subagent count 6→7 (commit `261878c`, Session 21)
- [x] Session 21 doc updates — CLAUDE.md (skills 5→6, agents 3→7, architecture tree, 2 Key Decisions rows for the build + 4th-dim extension, last-session bullets), README.md (file tree, Commands table, complement table positioning vs sibling skills, Subagents table), workflow.md (Quick Reference row, Command Reference section, Scenario 5 "First time in unfamiliar repo", May 2026 version-history row), MEMORY.md (counts) (Session 21)
- [x] Mechanical fix to `/plan-feature` `interview-rules.md:11` and `workflow.md:123` — "Batch 3-5 questions per call" → "Batch 3-4 questions per call" with cap explanation. Closes Next Steps #2 from Session 21 (which hit `AskUserQuestion` `too_big maximum: 4` error during the `/architecture-review` interview). (Session 22)
- [x] New `/code-health-advice` skill — read-only routing advisor (skill #7). Reads `git status`, branch, recent commits, ahead/behind counts, `gh pr view`, `CLAUDE.md` `In Progress`/`Next Steps`/`Last Updated` in a single parallel turn; classifies repo state into one of 5 buckets (A. pre-commit cleanup / B. pre-merge verification / C. post-ship audit / D. orient + audit / E. ambient improvement); prints a ~10-line report with state summary + bucket name + recommended flow + one alternative + optional notes. Three deliberate non-features: never invokes other skills, never edits files, exactly one Recommended + one Alternative per report. Tie-breakers codified: uncommitted-changes-wins, open-PR-beats-main, staleness-beats-recent-ship, no-CLAUDE.md-escalates-to-D. Files: `code-health-advice/SKILL.md` (103 lines), `code-health-advice/references/state-buckets.md` (169 lines). (Session 22)
- [x] `/code-cleanup --vulns` flag for dependency CVE scanning. Picked a flag on existing skill over a new `/audit-deps` skill — same scan path (audit the deps tree), zero new carrying cost. Three design choices: (1) opt-in only, default scans never call `npm audit` / `pip-audit` / etc., (2) multi-stack coverage (Node by lockfile / Python pip-audit→safety / Rust cargo audit / Go govulncheck / PHP composer audit / Ruby bundle audit), tool-missing emits an install-hint finding rather than silent zero, (3) never auto-fixed by `--fix` because version bumps can break apps and lockfile churn is project policy. Severity → risk mapping: critical/high → safe, moderate → likely_safe, low → needs_investigation. Files: `code-cleanup/SKILL.md` (frontmatter, dispatch, dashboard, fix-mode carve-out, scope table), `code-cleanup/references/scan-deps-config.md` new Section 4.5 (93 lines), `agents/cleanup-deps-config.md` (audit-tool Bash perms + opt-in rule). (Session 22)
- [x] Session 22 doc updates — CLAUDE.md (skills 6→7, Last Updated 2026-05-08, 2 new Key Decisions rows, replaced Session 21 last-session block with Session 22, architecture tree updated), README.md (commands table rows for `/code-health-advice` + `/code-cleanup --vulns`, "Not sure where to start?" callout, file tree), workflow.md ("Not Sure Where to Start" Quick Reference row, `/code-health-advice` Command Reference section, Scenario 6 "Not Sure Which Skill to Run", `/code-cleanup --vulns` example + Finds bullet, file tree, version history row), MEMORY.md (skill count 6→7). (Session 22)
- [x] Session 22 `/update-docs` Full Path housekeeping — Part 5 compressed Session 17 to one-liner (5 most recent now S18-S22), Part 6 rolled up the topmost Key Decisions row ("Don't filter claude-config from project picker") to `docs/key-decisions.md` per FIFO, Session 22 entry properly ordered after Session 21 in `docs/session-history.md` (Fast Path inserted in wrong order). (Session 22)
- [x] Session 23 `/code-health-advice` routes to `/test-review` — `SKILL.md` available-skills list + `references/state-buckets.md` Buckets C/D/E recommended flows updated. Buckets A/B intentionally skipped (diff/PR-scoped). Deliberate dangling pointer until S24 build. (Session 23)
- [x] **Built `/test-review` skill — repo-wide test suite audit (skill #8).** 3 Sonnet-routed subagents (`test-coverage`, `test-quality`, `test-economics`), 7 reference files (`test-smell-catalog.md` T01-T05, `scan-coverage.md`, `scan-quality.md`, `scan-economics.md`, `report-template.md`, `plan-mode-test.md`, `fix-mode-test.md`), 1 SKILL.md orchestrator (8-step). Twin headline metric (`Coverage gaps in critical code: X lines | Tests we can delete: Y lines`). `--fix` restricted to T01 (assertion-free, provably safe). `--coverage` opt-in per-stack (jest/vitest/pytest-cov/cargo-tarpaulin/go cover; never auto-invokes the tool). Non-overlap with `/code-cleanup` enforced in 12 places — defers entirely to `cleanup-styles-tests` §7 for orphans/skips/helpers/snapshots. 8 phase commits: `51a91d6` → `a52c3c7` → `6eeb332` → `9377e06` → `f22874c` → `e2a3a3b` → `1406c61` → `d379222`. (Session 24)
- [x] 3 new Sonnet subagents under `.claude/agents/` — `test-coverage` (ranks gaps by `security_keyword_density × log(churn) × log(import_fan_in)`; bug-fix-without-regression scan over last 50 commits; heuristic mode default, `--coverage` opt-in for report parsing), `test-quality` (walks T01-T05 catalog; project-defined assertion-helper allowlist scan runs FIRST as critical T01 false-positive guard), `test-economics` (snapshot-heavy ≥0.5 ratio, flakiness markers + git-log signals, test:code LOC ratio extremes; `deletable_lines > 0` only for snapshot_heavy to keep twin-headline math honest). Subagent count 7→10. (Session 24)
- [x] Session 24 doc updates — `README.md` (file tree adds `test-review/` + 3 `test-*.md` agents, commands table row, picker table row with twin headline, subagents table 3 rows, useful chain `/code-cleanup` → `/architecture-review` → `/test-review` → `/architecture-review --plan` → `/plan-feature`), `workflow.md` (Quick Reference "Test Suite Audit" row, new `/test-review` Command Reference section with `--coverage` opt-in policy and T01-only `--fix` design rationale, Scenario 5 step insertion, version history May 2026 row, custom commands tree). (Session 24)
- [x] **Built `/seo-review` skill — SEO + Generative Engine Optimization audit for web projects (skill #9).** 3 Sonnet subagents (`seo-technical` / `seo-content` / `geo-generative`); SKILL.md with 8-step orchestration; 8 references (`best-practices-sources.md`, `scan-technical.md`, `scan-content.md`, `scan-geo.md`, `rubric.md`, `fix-allowlist.md`, `report-template.md`, `plan-mode-seo.md`). **Three new skill patterns:** fetches best practices fresh every run (WebSearch + WebFetch from 4 source categories, parallel single turn); orchestrator-only HTTP boundary (subagents have no network tools); in-repo score history with delta in headline (`docs/seo-history.md`, append-only). Web-projects-only (rejects non-web silently via comprehensive auto-detect: frontend frameworks + server-rendered backends + static HTML). Sitemap URL probe (cap 100 URLs, 8-point score-impact cap; orchestrator-only HTTP). `--fix` strict allowlist NEVER fabricates content (11 patterns, all TODO-placeholder-only). `--url <deployed-url>` adds live HTML diff. Rubric: Technical SEO 25 / On-Page 25 / Structured Data 20 / Generative Engine 20 / Performance 10 = 100, with ±5-per-dim weight tuning from fetched brief (validation gate added in S25 /simplify pass). 6 build commits + 1 /simplify-pass: `dbcb820` → `301e9da` → `bd2c6f7` → `f9827e9` → `59a466a` → `46286e4` → `b6ca5fd`. (Session 25)
- [x] 3 new Sonnet subagents under `.claude/agents/` — `seo-technical` (Technical SEO 25 + Performance 10 = 35 pts; consumes orchestrator-passed sitemap URL probe results), `seo-content` (On-Page SEO 25 pts; strict no-fabrication on --fix), `geo-generative` (Structured Data 20 + Generative Engine 20 = 40 pts; fetched brief is source of truth, `brief_divergence` field surfaces heuristic disagreements). Subagent count 10→13. (Session 25)
- [x] `/code-health-advice` integration with `/seo-review` — lightweight `is_web` detection in Step 1; Bucket C (post-ship audit) and Bucket E (ambient improvement) recommended-flows route conditionally to `/seo-review` when web detected. Buckets A/B (diff/PR-scoped) and D (orient + audit on unfamiliar repos) deliberately unchanged. (Session 25)
- [x] Session 25 `/simplify` pass — 3 parallel review agents (reuse / quality / efficiency) surfaced 14 findings; fixed 10 (orphan sub_dim cleanup, Recipe/Event/Person fields added, weight-tuning validation gate, per-agent shared context scoping, history-write gating to default mode, parallel-turn wording tightening, scan-geo duplicate output-shape removal, vendored-dir alignment, Section 3.3 dedup); deferred 4 as Next Steps debt (batched-Grep alternation, fix-mode harness extraction, plan-mode scaffolding extraction, cross-file boilerplate consolidation). (Session 25)
- [x] Session 25 doc updates — `README.md` (agents tree adds 3 seo-* / geo-* agents alphabetized; skills tree adds `seo-review/` with 8 references; commands table row; picker table row with web-only-scope note; subagents table 3 rows; useful chain "(if web) /seo-review"). `workflow.md` (Quick Reference "SEO + GEO Audit" row; new `/seo-review` Command Reference section documenting the 3 deliberate design choices; custom commands tree; version history May 2026 row). (Session 25)
- [x] **`/code-review` pass on S25 build span (`dbcb820~1..HEAD`, 8 commits, 20 files, +2476 lines).** Fresh-context (post `/clear`) review per Writer/Reviewer pattern. Surfaced 3 Important findings (I1 missing `Write` in seo-review allowed-tools breaking first-run history creation; I2 sub-dim caps unenforced causing Section 1 breakdown overshoot; I3 orphan key-decisions.md table row from S25 Part 6 rollup) + 8 Suggestions captured in S26 session-history full prose. (Session 26)
- [x] **I1 fix — added `Write` to `seo-review/SKILL.md` `allowed-tools`.** First-run `docs/seo-history.md` creation now succeeds; `Edit` stays available for the append path on subsequent runs. 1-line frontmatter change. (Session 26)
- [x] **I2 fix — sub-dimension deduction clamp made canonical in one place.** Updated `seo-review/references/rubric.md` "Scoring computation" section with explicit `sub_dim_deduction = min(sum(score_impact for findings in this sub_dim), sub_dim_max)` formula + note that enforcement lives in orchestrator Step 6.1 + clarification that sub-dim caps stay at base values when weight adjustments shift dimension maxes. Rewrote `seo-review/SKILL.md` Step 6.1 from "Aggregate sub-dimension scores per dimension" → "Aggregate + clamp sub-dimension deductions" with explicit `clamped[sub_dim] = min(raw[sub_dim], sub_dim_max)` rule + divergence-flag fallback when a subagent's emitted dimension_total disagrees with the recomputed one; tightened Step 6.2 weight-adjustment-vs-sub-dim-cap wording. Subagents and scan files left untouched (single point of enforcement; avoids cap-table drift across 7 places). (Session 26)
- [x] Session 26 doc updates — `CLAUDE.md` (Last Updated S25→S26; In Progress paragraph updated; Next Steps #1 prepended with I1/I2 fix notes + 8-suggestion summary; new Next Steps #9 covering I3 fix + Part 6 regression investigation; replaced S25 last-session block with S26; pruned stale Previous Session blocks for S24/S22/S21 per the "one block only" rule — content already in `docs/session-history.md`). `docs/session-history.md` (S26 full prose entry; S18 rolled up to one-liner per Part 5). `docs/completed-work.md` (this entry). README/workflow not modified (no public-facing surface changes). (Session 26)
- [x] **/seo-review v2 — Phase 0: lock GSC contracts in `references/rubric.md`** (commit `0780223`). Score_impact:0 invariant + single point of orchestrator-side enforcement. 12 sub-dim enum for `gsc_insights` dimension. 8 new per-finding fields. Dual `effective_impact` ranking. URL dedup rules. `[gsc]` history-row prefix. (Session 27)
- [x] **/seo-review v2 — Phase 1: GSC CSV ingestion foundations** (commit `8db0fdf`). New `gsc-ingestion.md` (~450 lines canonical reference) + `gsc-setup-readme-template.md` (~150 lines user-facing). SKILL.md Step 0 detected-line expanded; new Step 1.6 with 7 sub-steps (detection, README+`.gitignore` idempotent setup, per-CSV parse, page_type_map + url_impressions_map, freshness, GSC-mode summary, shared-context block). (Session 27)
- [x] **/seo-review v2 — Phase 2: 35-day git-history awareness (Step 1.5)** (commit `790774b`). Universal SEO-relevant pathspec patterns + 17 stack-specific overlays. Single `git log --name-status` with `<<<COMMIT>>>` separator. 200-commit cap. Rename detection + cluster collapse. Shallow-clone graceful fallback. (Session 27)
- [x] **/seo-review v2 — Phase 3: scoring + ranking + report (contract-first)** (commit `6c2445a`). SKILL.md Step 6 refactored: 6.0 GSC consolidation passes (score_impact:0 enforcement + URL dedup + count); 6.6 dual `effective_impact` formula so GSC findings land in top-3; 6.7 source-aware filter. New report Section 3 GSC Insights (6 sub-blocks); renumbered Sections 4-6; footer additions. (Session 27)
- [x] **/seo-review v2 — Phase 4: seo-gsc-insights subagent** (commit `91a95a5`, ~140 lines after /simplify-pass dedup). Canonical 9-section structure. Pointer to `gsc-ingestion.md` catalog (single source of truth). `code_changed_since_gsc_window` annotation logic. Wired to Step 5 as 4th parallel Task dispatch. Subagent count 13 → 14. (Session 27)
- [x] **/seo-review v2 — Phase 5a: --plan integration** (commit `161761a`). Phase 1 renamed "Indexing & Crawl Hygiene"; bulk-redirect snippet for routing-rename + 404-cluster (asks user to confirm mapping, never fabricates); Phase 4 traffic-prioritized + 4.3 CTR opps + 4.4 position-band GSC-only sections. (Session 27)
- [x] **/seo-review v2 — Phase 5b: --fix display gains traffic_weight** (commit `be7b07f`). Per-finding gate displays traffic_weight when GSC enabled; apply-summary sorted by traffic_weight desc. No new fix-eligible patterns. (Session 27)
- [x] **/seo-review v2 — Phase 6: docs** (commit `6f8ff45`). SKILL frontmatter, README, Workflow, CLAUDE.md Key Decisions row. (Session 27)
- [x] **/seo-review v2 — /simplify pass** (commit `81369df`, net -157 lines). 3 parallel review agents flagged 22+ findings; fixed 16. F1: 168-line catalog dedup. Q6 real spec bug: Step 6.7 GSC-override silently dropped code_changed findings. Q1 field-name contradiction. Q4 sub-dim typo. Q5 banner-sentinel simplified. E1+E2+E9 explicit parallel-batch instruction. (Session 27)
- [x] **Session 27 doc updates + Next Steps #9 closed** — `/update-docs` Full Path. Part 5: S21+S22 rolled up. Part 6: 3 oldest decisions moved to `docs/key-decisions.md` + pre-existing orphan row from S25 relocated into table block. `mode-update.md` Step 6.3 patched with anchor-at-end-of-table rule. CLAUDE.md (In Progress + Next Steps + Session History + Key Decisions condensed to 20). (Session 27)
- [x] **/seo-review v3 — Phases 0-3 BigQuery integration** (commits `dedfd75`, `7486a62`, `3a2aad6`, `bffc510`, `bb0fdcf`, `385db25`). Schema lock + 3 BQ reference files; BQ→digest contract; URL Inspection + CrUX deferred-v3.x notes; bq CLI dispatcher + 4-state matrix + setup UX; README + Workflow + CLAUDE.md sweep. Plan-agent validation: 8 blindspots + 4 simplifications + 4 ordering improvements, all adopted. **Superseded same session by v3.x simplification** — see SIMPLIFY entry below. (Session 29)
- [x] **/seo-review v3.x — Phases 0-2 Search Console API integration** (commits `7ab15b9`, `58a1d62`, `1feadf0`). API digest contract; 2 new reference files (`gsc-api-schema.md` 430 lines + `gsc-api-queries.md` 390 lines); SKILL.md Step 1.6 dispatcher expansion + setup UX restructure for 3-path matrix. (Session 29)
- [x] **/seo-review SIMPLIFY — drop BigQuery + CSV paths, keep API only** (commit `95456a0`, +437/-1774). User-requested mid-Phase-3: "this is the most complicated skill of this repo. Keep only Search Console API." Deleted 3 BigQuery reference files; collapsed Step 1.6 from precedence-ladder + 4-label collapse to binary dispatcher; rewrote `gsc-ingestion.md` (916 → ~350 lines); collapsed setup banner from 3 paths to 1; removed `Bash(bq:*)`. Net: 12 ref files → 9. (Session 29)
- [x] **/seo-review /simplify cleanup** (commit `6d262dc`, +82/-91). Caught correctness bug: URL Inspection budget referenced Step 3.2 sitemap-probe-failures, but Step 3.2 runs AFTER Step 1.6 — rebalanced to 80-by-impressions + 20-git-changed. Stripped 20+ `(per Plan-agent BN/SN)` callouts; removed `byte-identical` framing (single source); fixed broken xref `page_type_map building` → `page_type_map sources`; updated rubric.md sub-dim source column. (Session 29)
- [x] **/seo-review /code-cleanup pass — Phase 3 docs sweep for API-only state** (commit `8fba1fa`, +18/-25). Phase 3 docs sweep was committed BEFORE the simplification — fixed all README/Workflow/CLAUDE.md references to the obsolete 3-path matrix. Removed 6 stale Mac-path bash allows from `.claude/settings.local.json`. (Session 29)
- [x] **Session 29 doc updates** — `/update-docs` Full Path. Part 5: 5 full-prose entries at cap (no rollup). Part 6: Key Decisions at 19 rows (no rollup). CLAUDE.md In Progress + Next Steps + Session History + Key Decisions row updated for v3 → v3.x arc. (Session 29)
- [x] **/seo-review header fix** (commit `835800d`, +137/-36). Surfaced during S30 dogfood prep: missing `x-goog-user-project` HTTP header on all GSC API calls returned 403 SERVICE_DISABLED. Skill now reads ADC quota project from `application_default_credentials.json:quota_project_id` (via `gcloud info` + jq, later replaced with grep+sed) and sends header on every call. Setup README restructured 3-step → 4-step. (Session 30)
- [x] **/seo-review jq → grep+sed extraction** (commit `bc1170a`, +16/-12). Bug #2 surfaced live in skill run: jq not on PATH in claude-code's Bash on Windows → silent extraction failure → empty `$QUOTA_PROJECT` → same 403. Switched to portable bash-core `grep -oE` + `sed -E` extraction across all 5 affected files. Allowed-tools updated: `Bash(jq:*)` → `Bash(grep:*)` + `Bash(sed:*)`. (Session 30)
- [x] **/seo-review ingestion conventions** (commit `bc35331`, +78/-6). 3 follow-ups from S30 dogfood codified into new cross-cutting "## Ingestion conventions" section in SKILL.md: disk-write boundary (`.seo-data/gsc/` reserved, `mktemp` for ephemeral parsing); JSON parser fallback chain (`jq` → `python` → `python3` → bash regex; Windows `python3` Microsoft-Store-stub caveat); budget utilization expectation (URL Inspection + sitemap probe caps are ceilings, not targets; literal top-N with no subjective trimming). Updated `gsc-api-queries.md` URL Inspection selection rule + SKILL.md Step 3.2 sitemap probe wording. (Session 30)
- [x] **/seo-review Phase 4 dogfood — end-to-end run on burakarik.com.** First real-world dogfood of v3.x API path on verified GSC property (`sc-domain:burakarik.com`, siteOwner). 40/100 score delivered with subtotal_check passing, 20 GSC info-only findings, 4th `seo-gsc-insights` subagent dispatched, Section 3 rendered. Surfaced 2 bugs + 3 follow-ups (all shipped same-session, see entries above). Watch-for checklist a/b/c/d/e/g ✅; f/h/i not tested. CLAUDE.md Next Step #1 ("dogfood /seo-review") removed. (Session 30)
- [x] **Session 30 doc updates** — `/update-docs` Full Path. Drift probes: session-history.md 5 full-prose entries at cap (no Part 5 rollup); Key Decisions 20 rows at cap (no Part 6 rollup); CLAUDE.md ~32k chars under 35k soft warn. Flagged: Session 27 entry missing from `docs/session-history.md` (lost in some prior run; commit `47464e2` preserves content). (Session 30)
- [x] **/seo-review cap-hit footer visibility** (commit `ada0353`, +40/-0). Surfaced from S30 dogfood observation that Q1+Q2 returned exactly 1,000 rows each (visually ambiguous between "site has 1000 unique candidates" vs "orchestrator improvised to API default of 1000 and lost data past cap"). SKILL.md Step 1.6.7 now captures rows_received vs rowLimit_requested per Q1/Q2/Q3; Step 1.6.12 footer emits one-line cap-hit summary with truncation warnings (Q3 cap-hit gets louder warning since it affects url_impressions_map completeness → traffic_weight ranking signal). gsc-api-queries.md adds "Cap-hit detection (per-call)" subsection. Pagination via startRow deliberately deferred until a real dogfood surfaces it on a large property. (Session 30 post-sweep)
- [x] **Google AI Optimization Guide source — added then walked back** (commits `af02e09` + `78f7e0f`, reverted by `aa95b47`). User shared Google's first-party AEO/GEO guide (published 2026-05-15). Added as priority best-practices source with corresponding Next Step #5 about rubric review (Google says llms.txt is unnecessary, structured data not required for generative AI, AI-specific rewrites unnecessary — direct conflicts with skill's static heuristics). User then deferred: "investigate later in focused session." Clean revert. Guide URL preserved in git history at `af02e09` for future revisit. Net: zero functional change; documented decision to defer engagement with substantive rubric implications. (Session 30 post-sweep)
- [x] **Session 30 cont. doc updates** — 2nd `/update-docs` Full Path sweep capturing the 3 post-sweep commits + 1 revert (`ada0353`, `af02e09`, `78f7e0f`, `aa95b47`). Same drift state as 1st sweep; no Key Decisions changes (AI-guide decision was deferred not adopted); CLAUDE.md grew from 35.6k → ~36.5k chars (under 40k hard cap but creeping — worth tightening pass next session). (Session 30)
